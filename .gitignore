node_modules
dist
dist-ssr
*.local

# Umgebungsvariablen - .env.example ist die eingecheckte Vorlage
.env
.env.*
!.env.example

# Rust / Tauri
src-tauri/target
src-tauri/gen/schemas

# Android (wird von `tauri android init` erzeugt)
src-tauri/gen/android/.gradle
src-tauri/gen/android/local.properties
src-tauri/gen/android/app/build
src-tauri/gen/android/build
*.keystore
keystore.properties

# Claude Code: settings.json waere geteilt, settings.local.json ist
# maschinenlokal (erteilte Berechtigungen o. ae.) und gehoert nicht ins Repo.
.claude/settings.local.json

# Editoren / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Build-Zwischenstände
*.tsbuildinfo

# Tauri-Signaturschluessel: privat, gehoert NIE ins Repo.
# Der oeffentliche Teil steht in src-tauri/tauri.conf.json.
.tauri/
